//! `sentinel-store` — `Store` port implementation (SQLx). **Post-MVP (Phase 2).**
//!
//! Persists runs / results and (later) baselines. Depends on `sentinel-core` and implements its
//! `Store` port. Placeholder crate for now; the MVP runs stateless and writes file reports.
