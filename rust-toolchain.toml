# Pinned toolchain for reproducible local + CI builds. Bump to track latest stable.
# Distinct from the workspace MSRV (`rust-version = "1.85"` in Cargo.toml): that is the
# minimum we support, this is what we actually build and test with.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
